document.addEventListener("DOMContentLoaded",()=>{const t=document.querySelector(".menu-toggle"),n=document.querySelector(".nav");if(t&&n)t.addEventListener("click",()=>{const o=n.classList.toggle("open");t.setAttribute("aria-expanded",String(o))});if(typeof posts!=="undefined"){if(document.getElementById("qa-list"))renderPosts(posts);if(document.getElementById("home-posts"))renderHomePosts(posts.slice(0,3))}document.querySelectorAll(".filter").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll(".filter").forEach(x=>x.classList.remove("active"));b.classList.add("active");const f=b.dataset.filter;renderPosts(f==="all"?posts:posts.filter(p=>p.tags.includes(f)))}))});function renderPosts(items){const l=document.getElementById("qa-list");if(!l)return;l.innerHTML=items.map(p=>`<article class="post-card" id="${p.id}"><div class="post-meta"><span>${p.category}</span><span>NaRye Q&A</span></div><h2>${p.title}</h2><p>${p.preview}</p><button class="read-more" data-post="${p.id}">Read answer →</button><div class="post-content" id="content-${p.id}" hidden>${p.content}</div></article>`).join("");l.querySelectorAll(".read-more").forEach(b=>b.addEventListener("click",()=>{const x=document.getElementById("content-"+b.dataset.post),o=!x.hidden;x.hidden=o;b.textContent=o?"Read answer →":"Close answer ↑"}))}function renderHomePosts(items){const h=document.getElementById("home-posts");if(!h)return;h.innerHTML=items.map(p=>`<article class="mini-post"><span>${p.category}</span><h3>${p.title}</h3><p>${p.preview}</p><a href="qa.html#${p.id}">Read →</a></article>`).join("")}
